import React from 'react';
// import { Link } from 'gatsby';
import './Menu.scss';

interface Props {
  siteTitle?: string;
}

const Menu: React.FunctionComponent<Props> = (props: Props) => (
  <>
    <div>
      Menu items
    </div>
  </>
);

export default Menu;
